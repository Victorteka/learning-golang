package mainimport "fmt"// adding data to an array// can not append if the array is already fullfunc append() {     slice1 := []int{1,2,3}     slice2 := append(slice1, 4, 5)     fmt.Println(slice1, slice2)}// copying another array// If the result array is shorter, only the values that fit into the array// will be copied into itfunc copy() {     slice1 := []int{1,2,3}     slice2 := make([]int, 2)     copy(slice2, slice1)     //  After running this program slice1 has [1,2,3] and slice2 has [1,2].     //  The contents of slice1 are copied into slice2,     //  but since slice2 has room for only two elements only the first two elements of slice1 are copied.     fmt.Println(slice1, slice2)}func main() {    append()}